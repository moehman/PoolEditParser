To run the program, you need to copy the libexpat.dll file from the
lib directory somewhere so that Windows can find it. The easiest way
is to copy it to the same directory with the program executable.
